v2.20 backend scaffold integrated into current repo.

What changed:
- Added user_roles table/model and runtime migration.
- Varaxian is forced to super_admin.
- Added require_admin and require_super_admin guards.
- Added /admin routes for ingest, scheduler, sessions, users, and role changes.
- Protected existing manual ingest, scheduler, and platform settings mutations behind admin auth.
- Added separate UI pages: /dashboard, /active-opportunities, /admin.

Important:
- Existing user_sessions table is retained as the session store.
- Core ingest, opportunity math, and BIGINT location IDs were left in place.
